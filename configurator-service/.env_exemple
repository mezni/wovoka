# =============================================
# Database Configuration
# =============================================
DB_NAME=ev_db
DB_USER=postgres
DB_PASSWORD=password
#DB_SERVICE=postgis
DB_SERVICE=localhost
#DATABASE_URL=postgresql://${DB_USER}:${DB_PASSWORD}@${DB_SERVICE}:5432/${DB_NAME}
DATABASE_URL=postgresql://postgres:password@localhost:5432/ev_db

DATABASE_MAX_CONNECTIONS=20
DATABASE_CONNECTION_TIMEOUT=30

# =============================================
# Server Configuration  
# =============================================
HOST=0.0.0.0
PORT=8080
SERVER_WORKERS=4

# =============================================
# Logging Configuration
# =============================================
LOG_LEVEL=debug
RUST_LOG=networks_service=debug,actix_web=info,sqlx=warn

# =============================================
# Application Settings
# =============================================
APP_NAME=Networks Service
APP_VERSION=1.0.0
API_PREFIX=/api/v1

# =============================================
# CORS Configuration (if needed)
# =============================================
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000